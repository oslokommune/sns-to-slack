handler: slack.lambda_to_slack.lambda_handler
environment:
  STAGE: ${self:provider.stage}
events:
 - http:
    path: "post-to-slack"
    method: get
    cors: { origin: '*' }
    request:
      parameters:
        paths:
          uuid: true
