handler: slack.cloudwatch_pipeline_state_to_slack.handler
environment:
  STAGE: ${self:provider.stage}
  SLACK_ALERTS_WEBHOOK_URL: ${ssm:/dataplatform/msg-to-slack/slack-alerts-webhook-url~true}
events:
  - sns: arn:aws:sns:eu-west-1:#{AWS::AccountId}:dataplatform_state_machine_event
