handler: slack.cloudwatch_error_to_slack.lambda_handler
environment:
  STAGE: ${self:provider.stage}
  SLACK_WEBHOOK_URL: ${ssm:/dataplatform/slack/cloudwatch-lambda-error/url}
events:
  - sns: arn:aws:sns:eu-west-1:#{AWS::AccountId}:dataplatform_lambda_error
